// For simplicity, we'll use an in-memory store for this example
// In a production environment, you would connect to a real database

let users = [];
let elections = [];
let votes = [];

module.exports = {
  users,
  elections,
  votes
};